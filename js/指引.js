
let div=document.querySelectorAll("div")
let sum0=""
for(let i=0;i<5;i++){
    if(i==1 || i==3)
        sum0+=1 
     else
    sum0+=0
    }
 div[0].innerHTML+=sum0
    div[0].innerHTML+="\t"
    sum0=""
    for(let i=0;i<5;i++){
    if(i==1 || i==3)
        sum0+=0 
     else
    sum0+=1
    }
 div[0].innerHTML+=sum0
    div[0].innerHTML+="\t"
    sum0=""
    for(let i=0;i<5;i++){
    if(i==2)
        sum0+=1 
     else
    sum0+=0
    }
div[0].innerHTML+=sum0
    div[0].innerHTML+="\t"
let sum="";
for(let i=0;i<52.0;i++){
    if(i<26 || i>26)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
    for(let i=0;i<52.0;i++){
    if(i<24 || i>28)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\n"
     sum="";
     for(let i=0;i<52.0;i++){
    if(i<21 || i>31)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t \n"
     
   sum="";
    for(let i=0;i<51.0;i++){
    if(i<17 || i>35)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";for(let i=0;i<52.0;i++){
    if(i<12 || i>40)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";

    for(let i=0;i<52.0;i++){
    if(i<6 || i>45)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";for(let i=0;i<52.0;i++){
        if(i<2 || i>49)
        sum+=1
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";

    for(let i=0;i<52.0;i++){
    // if(i<21 || i>26)
    //     sum+=1 
    //  else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
     for(let i=0;i<52.0;i++){
        if(i<22 || i>25)

        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";

     for(let i=0;i<52.0;i++){
        if(i<22 || i>25)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
     for(let i=0;i<52.0;i++){
        if(i<22 || i>25)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
     for(let i=0;i<52.0;i++){
        if(i<22 || i>25)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
     for(let i=0;i<52.0;i++){
        if(i<22 || i>25)
        sum+=1
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
     for(let i=0;i<52.0;i++){
        if(i<22 || i>25)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
     for(let i=0;i<52.0;i++){
        if(i<22 || i>25)
        sum+=1 
     else
    sum+=0
    }
    div[0].innerHTML+=sum
    div[0].innerHTML+="\t"
     sum="";
